{
  "name": "sql-employee-tracker",
  "version": "1.0.0",
  "description": "A command-line application from scratch to manage a company's employee database, using Node.js, Inquirer, and PostgreSQL.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "npm run build && node index.js",
    "reset": "psql -U lydia.ogles -d sql_employee_tracker -f ./db/schema.sql && psql -U lydia.ogles -d sql_employee_tracker -f ./db/seeds.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "inquirer": "^12.3.2",
    "node": "^18.20.6",
    "pg": "^8.13.1",
    "typescript": "^5.7.3"
  },
  "type": "module"
}
